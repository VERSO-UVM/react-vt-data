{
  "default": {
    "zoning_info": {
      "join_key": "OBJECT_ID",
      "join_type": "inner",
      "columns": {
        "County": "County",
        "Jurisdiction": "Municipal_Name",
        "District Type": "District_Type",
        "District Name": "District_Name"
      }
    },
    "zoning_rules": {
      "join_key": "OBJECT_ID",
      "join_type": "inner",
      "columns": {
        "Usage Type": "use_type",
        "Rule": "rule",
        "Value": "val"
      }
    },
    "cdc_places": {
      "join_key": "LocationID",
      "join_type": "inner",
      "columns": {
        "Key Indicator": "SME_Highlight",
        "Category": "Category",
        "Measure": "Measure"
      },
      "range": { "Percent": "Data_Value" }
    }
  }
}
